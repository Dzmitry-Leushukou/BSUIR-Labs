<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Lab_1.Lab3"
             Title="Lab 3">
    <VerticalStackLayout>
        <Picker 
                x:Name="routePicker"
                Title = "Routes"     
                ItemDisplayBinding = "{Binding Name}"
                SelectedIndexChanged="OnRouteSelected"
        />
        <CollectionView x:Name="attractionView">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Label HeightRequest="25" FontSize="Small">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="{Binding Name}"/>
                                    <Span Text=" ("/>
                                    <Span Text="{Binding Rating}"/>
                                    <Span Text=" ✯)"/>
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

    </VerticalStackLayout>
</ContentPage>